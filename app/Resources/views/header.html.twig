<nav class="navbar navbar-inverse">
    <div class="container">
        <div class="navbar-header">
            <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navigation-menu"
                    aria-expanded="false">
                <span class="sr-only">Toggle navigation</span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
            <a class="navbar-brand" href="{{ path('homepage') }}">Pegase</a>
        </div>


        <div class="collapse navbar-collapse" id="navigation-menu">
            <ul class="nav navbar-nav">
                <li {% if activeWorldTours is defined %}class="active"{% endif %}><a href="{{ path('voyages') }}">Tours du monde</a></li>
            </ul>

            {% if is_granted('IS_AUTHENTICATED_FULLY') %}
                <ul class="nav navbar-nav">
                    <li {% if activeDashboard is defined %}class="active"{% endif %}><a href="{{ path('dashboard') }}">Tableau de bord</a></li>
                </ul>

                <span id="navBarInfos"></span>

                <ul class="nav navbar-nav navbar-right">
                    <li class="dropdown {% if activeProfile is defined %}active{% endif %}">
                        <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true"
                           aria-expanded="false">
                            {{ app.user.username }}&nbsp;&nbsp;&nbsp;<i class="fa fa-cog"></i> <span class="caret"></span>
                        </a>
                        <ul class="dropdown-menu">
                            <li><a href="#">Profil</a></li>
                        </ul>
                        <ul class="dropdown-menu">
                            <li><a href="{{ path('fos_user_security_logout') }}">Deconnexion</a></li>
                            <li><a href="{{ path('fos_user_profile_edit') }}">Modifier votre profil</a></li>
                            <li><a href="{{ path('fos_user_change_password') }}">Changer de mot de passe</a></li>
                        </ul>
                    </li>
                </ul>
            {% else %}
                <ul class="nav navbar-nav navbar-right">
                    <li><a href="#" data-toggle="modal" data-target="#connectionModal">
                            <i class="fa fa-sign-in"></i> Connexion</a></li>
                    <li><a href="{{ path('fos_user_registration_register') }}">
                            <i class="fa fa-user"></i> Inscription</a></li>
                </ul>
            {% endif %}
        </div>

    </div>
</nav>

<nav id="siteHeader" class="navbar navbar-inverse">
    <div class="container-fliud">
        <div class="col-lg-10 col-lg-offset-1">
        <div class="navbar-header">
            <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navigation-menu"
                    aria-expanded="false">
                <span class="sr-only">Toggle navigation</span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
            <a class="" href="{{ path('homepage') }}">
                <img class="imgIcon" src="{{ asset('icon.png') }}" alt="Icone le monde en sac" ><img class="imgTitle" src="{{ asset('title.png') }}" alt="Titre le monde en sac" >
            </a>
        </div>


        <div class="collapse navbar-collapse" id="navigation-menu">
            <ul class="nav navbar-nav">
                <li {% if activeDestinations is defined %}class="active"{% endif %}><a href="{{ path('destinationsList') }}" alt="Liste des destinations">Destinations</a></li>
                <li {% if activeIdeasOfTravels is defined %}class="active"{% endif %}><a href="{{ path('ideasOfTravels') }}" alt="Idées de voyage">Idées de voyage</a></li>
            </ul>

            {% if app.user is not null and is_granted('ROLE_USER') %}
                <ul class="nav navbar-nav">
                    <li {% if activeDashboard is defined %}class="active"{% endif %}>
                        <a href="{{ path('voyages') }}">Tableau de bord</a>
                    </li>
                    <li><a target="_blank" alt="Blog" href="http://blog.lemondeensac.com/">Blog</a></li>
                </ul>

                <ul class="nav navbar-nav navbar-right">
                    <li class="dropdown {% if activeProfile is defined %}active{% endif %}">
                        <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true"
                           aria-expanded="false">
                            {{ app.user.username }}&nbsp;&nbsp;&nbsp;<i class="fa fa-cog"></i> <span class="caret"></span>
                        </a>
                        <ul class="dropdown-menu">
                            <li><a href="#">Profil</a></li>
                        </ul>
                        <ul class="dropdown-menu">
                            <li><a href="{{ path('fos_user_security_logout') }}">Deconnexion</a></li>
                            <li><a href="{{ path('fos_user_profile_edit') }}">Modifier votre profil</a></li>
                            <li><a href="{{ path('fos_user_change_password') }}">Changer de mot de passe</a></li>
                        </ul>
                    </li>
                </ul>
            {% else %}
                <ul class="nav navbar-nav">
                    <li><a target="_blank" alt="Blog" href="http://blog.lemondeensac.com/">Blog</a></li>
                </ul>

                <ul class="nav navbar-nav navbar-right">
                    <li {% if activeConnection is defined %}class="active"{% endif %}>
                        <a href="#" data-toggle="modal" data-target="#connectionModal">
                            <i class="fa fa-sign-in"></i> Connexion</a>
                    </li>
                    <li {% if activeRegister is defined %}class="active"{% endif %}>
                        <a href="{{ path('fos_user_registration_register') }}">
                            <i class="fa fa-user"></i> Inscription</a></li>
                </ul>
            {% endif %}
        </div>
        </div>
    </div>
</nav>
